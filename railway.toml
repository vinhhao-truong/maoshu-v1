[build]
builder = "NIXPACKS"
buildCommand = "cd apps/backend && npm install --include=dev && npm run build && cp -r .medusa/server/public . && npm prune --omit=dev"

[deploy]
startCommand = "cd apps/backend && npm run start"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
